# The URL the site will be built for
base_url = "https://blog.lucasholten.com"

title = "Blog by LHolten"
description = "Rust projects etc"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "abridge"

taxonomies = [
  { name = "tags" },
]

default_language = "en"

generate_feeds = true

[markdown]
highlight_code = true
highlight_theme = "css"


[extra]
mastodon = "https://hachyderm.io/@lholten"
github = "LHolten"
feed = true
